# L'Esprit Léger Android V5

Application Android emballant la version stable finale bêta V5 de L'Esprit Léger.

Le workflow GitHub Actions `build-apk.yml` compile automatiquement un APK debug installable à chaque push sur `main`.
